%p Add a new Category
=simple_form_for [:admin, @category] do |f|
  
  %p
    = f.input :name
  %p
    = f.input :picture, label: "Picture new"
  %p
    = f.label "Parent"
    / = f.select :ancestry, Category.all.collect {|p| [ p.name, p.id ] }, { include_blank: true }
    = f.collection_select :parent_id, Category.order(:name), :id, :name, { include_blank: true }
  %p
    = f.submit "Add category"